:php
  /**
   * Template part for displaying 'Aerophoto Application' block
   *
   * @package aero
   *
   */

  $args = [
    'post_type' => 'page',
    'post_status' => 'publish',
    'pagename' => 'aerophoto-application',
  ];

  $the_query = new WP_Query( $args );

  if ( $the_query->have_posts() ) :

include ../../pug/mixins.pug

section.page-aerophoto__application.aerophoto-application
  :php
    /* Start the Loop */
    while ( $the_query->have_posts() ) : $the_query->the_post();
  +or('<h2 class="aerophoto-application__title"><?php the_title() ?></h2>')
  :php
    the_content();
    endwhile;
|
|
<?php endif; ?>
